Drop your logo files here:

- `taxedhq-logo-transparent.png` (preferred for navbar/footer on dark backgrounds)
- `taxedhq-logo-with-bg.png` (optional fallback/background version)

Current UI wiring:

- Navbar + Footer load `taxedhq-logo-transparent.png`
- If missing or unreadable, the app falls back to the previous icon + text logo
